---
id: pn-07a0
status: completed
deps: []
links: []
created: 2026-01-29T08:07:30Z
type: task
priority: 1
assignee: lrepo52
parent: pn-fed6
---
# Update go package template - ensure devbox.json, files/ structure, .envrc, README.md, AGENTS.md, copier.yml compliance

## Completed

**2026-01-29T08:30:00Z**

✅ Updated Go package template to comply with Rust template standards:
- Created proper `files/` directory structure with Jinja2 templates
- Updated `copier.yaml` with `_subdirectory: files` and `_templates_suffix: .jinja`
- Added comprehensive `devbox.json.jinja` with development environment configuration
- Added `.envrc.jinja` for direnv environment setup
- Added `README.md.jinja` with complete documentation
- Added `AGENTS.md.jinja` with agent instructions
- Added `LICENSE.jinja` with support for MIT, Apache-2.0, AGPL-3.0, and MPL-2.0
- Added `Dockerfile.jinja` with multi-stage build and security best practices
- Added `.dockerignore.jinja` for optimized Docker builds
- Added `.gitignore.jinja` for Go projects
- Created library package structure in `pkg/{{package_slug}}/`
- Added comprehensive test files with testify
- Added conditional support for CLI, HTTP, PostgreSQL, Redis, and SQLite
- Used `git mv` for file moves to preserve history

The template now matches the Rust template structure and includes all required files.
