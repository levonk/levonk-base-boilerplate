# {{category}}/{{domain}}-{{package_name}} (clang package)

{{package_description}}

This template scaffolds a small C++/clang package using CMake. It is designed for
mocking or helper libraries (for example, local mocks for the Windhawk API)
inside the monorepo.

## Generated layout

- `CMakeLists.txt` --- Minimal CMake project configured for C++17.
- `src/main.cpp` --- Simple entry point or library stub.

After generation, customize the targets (library vs executable) as needed for the
specific package.
