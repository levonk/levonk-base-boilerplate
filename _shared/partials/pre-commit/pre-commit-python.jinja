# Python: ruff for lint + format (self-contained, no per-project types needed).
# Add mypy per-project once types stubs are pinned.
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
    -   id: ruff
        args: [--fix]
    -   id: ruff-format
