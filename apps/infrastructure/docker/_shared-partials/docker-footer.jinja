# Docker Configuration Footer
# This partial is shared across all Docker templates

---
## Build and Deployment Information

### Build Commands
```bash
# Build the Docker image
docker build -t {{ docker_registry | default('local') }}/{{ project_name | default('project') }}:{{ docker_tag | default('latest') }} {{ build_context | default('.') }}

# Run the container
docker run -d --name {{ project_name | default('project') }}-container {{ docker_registry | default('local') }}/{{ project_name | default('project') }}:{{ docker_tag | default('latest') }}
```

### Configuration Summary
- **Project**: {{ project_name | default('Docker Project') }}
- **Environment**: {{ environment | default('development') }}
- **Platform**: {{ platform | default('linux/amd64') }}
- **Base Image**: {{ docker_image | default('ubuntu:latest') }}
- **Registry**: {{ docker_registry | default('local') }}

---
Generated by Copier Docker Templates
Template Path: {{ _copier_answers._template_path | default('unknown') }}
Timestamp: {{ now() }}
