# Docker Configuration Header
# This partial is shared across all Docker templates

# {{ project_name | default('Docker Project') }}
Environment: {{ environment | default('development') }}
Generated: {{ now() }}

# Docker Image Configuration
Base Image: {{ docker_image | default('ubuntu:latest') }}
Registry: {{ docker_registry | default('local') }}
Tag: {{ docker_tag | default('latest') }}

# Build Information
Build Context: {{ build_context | default('.') }}
Dockerfile: {{ dockerfile_name | default('Dockerfile') }}
Platform: {{ platform | default('linux/amd64') }}
