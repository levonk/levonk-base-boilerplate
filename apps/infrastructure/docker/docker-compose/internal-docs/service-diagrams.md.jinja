# {{ _service_name | title }} Compose Topology Diagrams

⭐ **Purpose**

Document the default interaction model for the generated docker-compose stack. Tailor these diagrams after scaffolding to match any additional services, volumes, or networks you introduce.

☑️ **Mermaid Flow**

See [service-diagram.mmd.jinja2](service-diagram.mmd.jinja2).

☑️ **Mermaid Sequence**

See [sequence-diagram.mmd.jinja2](sequence-diagram.mmd.jinja2).

✅ **Implementation Notes**

- Swap the `External dependency` placeholder with real systems (database, queue, third-party API) once known.
- Extend both diagrams when adding additional containers (workers, schedulers, migrations) so operational docs match the compose file.
- Keep diagrams synchronized with `docker-compose.yml.jinja2`, `.override` files, and supporting Makefile targets.
- After customization, export diagrams to SVG/PNG via `mmdc` to include in READMEs or internal presentations.
