_min_copier_version: "9.0.0"
_subdirectory: files
_templates_suffix: .jinja

package_name:
  type: str
  help: "Project name"
  default: "test-docker-project"

environment:
  type: str
  help: "Environment"
  default: "development"

docker_image:
  type: str
  help: "Docker base image"
  default: "ubuntu:latest"
