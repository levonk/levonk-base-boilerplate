# Pre-commit hooks configuration.
# Generated from levonk-base-boilerplate shared partials; extend per-project as needed.
# Install: pre-commit install && pre-commit run --all-files
repos:
{% include "partials.bak/partials/pre-commit/pre-commit-common.jinja" %}
{% include "partials.bak/partials/pre-commit/pre-commit-docker.jinja" %}
# yamllint listed explicitly (not nested in the ansible/docker partial above):
# nesting would risk duplicate yamllint if a project composes multiple yaml-bearing partials.
{% include "partials.bak/partials/pre-commit/pre-commit-yaml.jinja" %}
