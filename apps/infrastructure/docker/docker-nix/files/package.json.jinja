{
  "name": "docker-nix",
  "version": "1.0.0",
  "description": "Docker Nix container with NX monorepo support",
  "scripts": {
    "build": "docker compose build",
    "build:all": "docker compose build",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "clean": "docker compose down -v"
  },
  "devDependencies": {
    {% include "partials.bak/partials/package-json-partials/infrastructure-nx-dev-deps.partial.json.jinja" %}
    "@nx-tools/nx-container": "catalog:"
  },
  {% include "partials.bak/partials/package-json-partials/package-json-tooling.partial.json.jinja" %}
  "modeline": "/* vim: set ft=json: */"
}
