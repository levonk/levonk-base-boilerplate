{
  "name": "{{ project_name | default('airflow-task') }}",
  "version": "1.0.0",
  "description": "Airflow Node task container with NX monorepo support",
  "scripts": {
    "build": "docker build -t {{ project_name | default('airflow-task') }} .",
    "docker:build": "docker build -t {{ project_name | default('airflow-task') }} .",
    "docker:push": "docker push {{ project_name | default('airflow-task') }}"
  },
  "devDependencies": {
    {% include "partials.bak/partials/package-json-partials/infrastructure-nx-dev-deps.partial.json.jinja" %}
    "@nx-tools/nx-container": "catalog:"
  },
  {% include "partials.bak/partials/package-json-partials/package-json-tooling.partial.json.jinja" %}
  "modeline": "/* vim: set ft=json: */"
}
