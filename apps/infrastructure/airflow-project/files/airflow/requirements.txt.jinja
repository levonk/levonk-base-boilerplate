# Airflow project extras (DAG-side), not the Airflow core itself.
# Install these into your Airflow environment (webserver/scheduler image)
# pin providers compatible with your Airflow version.

apache-airflow-providers-cncf-kubernetes>=10.22.0
requests>=2.34.2
pydantic>=2.13.5
