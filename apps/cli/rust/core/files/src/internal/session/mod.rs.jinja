//! Session context and hook infrastructure
//! 
//! This module provides session context generation for ambient context injection
//! and hook registration for agent integration (Claude Code, Codex, etc.).

pub mod context;
pub mod hooks;
pub mod metadata;
pub mod tests;

pub use context::{SessionContext, SessionContextBuilder, generate_session_context};
pub use hooks::{HookRegistry, HookPlatform, install_agent_hooks, register_session_hook};
pub use metadata::{SessionMetadata, capture_session_metadata};
