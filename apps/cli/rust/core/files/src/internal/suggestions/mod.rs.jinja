//! Contextual suggestion engine for CLI commands
//! 
//! This module provides intelligent, context-aware suggestions that help users
//! discover CLI capabilities organically by using the tool. Suggestions are
//! formatted as structured `help[]` arrays in TOON output.

pub mod engine;
pub mod rules;

#[cfg(test)]
mod integration_tests;

pub use engine::{SuggestionEngine, SuggestionContext, Suggestion, SuggestionRank};
pub use rules::{SuggestionRule, RuleRegistry, get_default_rules};
