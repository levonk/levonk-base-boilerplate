//! Mode detection and selection
//! 
//! This module handles automatic detection of CLI execution mode (agent vs human)
//! based on environment, TTY presence, and explicit configuration.

pub mod detection;
pub mod tests;

pub use detection::{Mode, ModeDetection, ModeSource, detect_mode};
