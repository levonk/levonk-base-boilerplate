//! Schema module for output field selection
//!
//! This module provides schema definitions and field selection logic
//! for controlling CLI command output to reduce token consumption in agent mode.

pub mod definition;
pub mod selector;
pub mod schemas;

pub use definition::{OutputSchema, FieldDefinition, SchemaRegistry};
pub use selector::{FieldSelector, parse_fields_arg, apply_list_limit, apply_schema_transforms};
pub use schemas::create_default_schema_registry;
