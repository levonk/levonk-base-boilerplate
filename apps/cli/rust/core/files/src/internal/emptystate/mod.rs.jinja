//! Empty state module for definitive empty result formatting
//! 
//! This module provides functionality to detect and format empty states
//! for CLI commands, ensuring agents understand when queries return no results
//! without re-running commands with different flags.

pub mod detector;
pub mod formatter;

#[cfg(test)]
mod tests;

pub use detector::{EmptyStateDetector, EmptyStateContext};
pub use formatter::{EmptyStateFormatter, EmptyStateMessage};
