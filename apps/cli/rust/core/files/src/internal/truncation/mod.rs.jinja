//! Content truncation module for reducing token consumption
//! 
//! This module provides truncation functionality for large text fields
//! to reduce token consumption in agent mode while providing escape hatches
//! for retrieving full content when needed.

pub mod truncator;
pub mod metadata;

#[cfg(test)]
mod tests;

pub use truncator::Truncator;
pub use metadata::{TruncationMetadata, TruncationInfo};
