fn main() {
    // Generate version info with vergen
    vergen::EmitBuilder::builder()
        .all_build()
        .all_git()
        .git_commit_date()
        .git_sha(false)
        .emit()
        .expect("Failed to emit vergen directives");
}
