version: '3.8'

services:
  cli:
    build: .
    image: {{ project_slug }}:latest
    entrypoint: ["python", "-m", "{{ project_slug }}"]
    volumes:
      - .:/app
    # Keep container running if needed, or just run one-off
    # command: ["--help"]
