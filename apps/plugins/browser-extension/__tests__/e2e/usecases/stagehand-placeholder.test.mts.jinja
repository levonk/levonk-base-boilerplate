import { test } from "@playwright/test";

// Placeholder for Stagehand-powered tests.
//
// To enable, install and configure @browserbasehq/stagehand with an API key,
// then replace this file with real Stagehand flows.
//
// This file is skipped by default so it will not fail CI.

test.skip(true, "Stagehand sample not implemented; replace with real tests when ready");

test("stagehand placeholder", async () => {
  // Intentionally empty; see comments above.
});
