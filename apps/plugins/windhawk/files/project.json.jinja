{
  "name": "{{ package_name }}",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": ".",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Windhawk mods are compiled by the Windhawk engine — no local build target'",
        "cwd": "{projectRoot}"
      }
    }
  }
}
